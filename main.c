#include<stdio.h>
#include "func.h"
#define MAX(a,b) ((a) > (b) ? (a) : (b))

int main(){
    int a, b, num, max;
    num=100;
    max=MAX(a,b);
    printf("%d",numSumaF(num));
    printf("\n");
    printf("%d",numSumG(num));
    printf("\n");
    return 0;
}